---
  - name: install wireguard dependencies
    hosts: "*"
    tasks:
      - name: install dependencies
        apt: 
          name: wireguard
          state: present
          update_cache: true
        become: true

    